#If we list all the natural numbers below 10 that are multiples of 3 or 5, we get 3,5,6 and 9. The sum of these multiples is 23.
#Find the sum of all the multiples of 3 or 5 below 1000.
#phyton codes:

sum=0
for i in range(1,1000): #1000'e kadar 1 er artan döngü
    if i%3==0 or i%5==0: #3 ve 5 e tam bölündüğünde toplam değeri i kadar arttır.
        sum+=i
    else:  #koşul sağlanmaz ise i yi 1 arttır ve döngüye devam et.
        i+=1
print(sum)
